# The seeded canvas is generated: ~2.4MB, almost all of it the design
# editor's own payload. The artboards and canvas.json beside it are the
# source — re-seeding from those rebuilds it.
truckers-trokeros-directions.html
